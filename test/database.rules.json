{
  "rules": {
    "siteStats": {
      "pageViews": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.isNumber() && newData.val() >= 0"
      }
    },
    "sitePresence": {
      ".read": "auth != null",
      "$session": {
        ".write": "auth != null && ((!data.exists() && (!newData.exists() || newData.child('uid').val() === auth.uid)) || (data.exists() && data.child('uid').val() === auth.uid))"
      }
    },
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}